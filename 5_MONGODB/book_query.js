// db.book.find({})

// db.book.find({"pages":{$eq : 505}})

// db.book.find({"pages":{$ne : 505}})

// db.book.find({"pages":{$gt : 505}})

// db.book.find({"pages":{$lt : 505}})

// db.book.find({"pages":{$lte : 505}})

// db.book.find({"pages":{$gte : 505}})

// db.book.find({"pages":{$in : [505 , 384 , 320]}})

// db.book.find({"pages":{$nin : [505 , 384 , 320]}})